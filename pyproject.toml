[project]
name = "ChronoRay"
version = "0.1.0"
dependencies = [
    "ray",
    "ray[tune]",
    "numpy",
    "bayesian-optimization==1.4.3",
]

[tool.setuptools.packages.find]
include = ["ChronoRay*"]
